{
  "name": "apostai-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd project && npm run build",
    "dev": "cd project && npm run dev",
    "install": "cd project && npm install"
  },
  "workspaces": [
    "project"
  ]
}
